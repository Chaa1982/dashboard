import {FC} from "react";
import {FlexColumn} from "../../shared/Flex";

export const Dashboard: FC = () => {
    return (
        <FlexColumn width={'100%'}>
            <h1>DASHBOARD</h1>
        </FlexColumn>
    )
};